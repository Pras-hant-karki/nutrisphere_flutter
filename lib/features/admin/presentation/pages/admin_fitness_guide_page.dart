import 'package:flutter/material.dart';

class AdminFitnessGuidePage extends StatelessWidget {
  const AdminFitnessGuidePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Admin Fitness Guide Page')),
    );
  }
}